# Employee Handbook Agent Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
agno         # Agent framework (includes LanceDB)
pypdf        # PDF file reading
pandas       # Data processing
fastembed    # FREE local embedder (no OpenAI API key needed!)

# Installation command:
# pip install -U agno pypdf pandas fastembed

